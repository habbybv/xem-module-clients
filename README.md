# xem-module-clients

External Clients module package for XEM core.

## Purpose
Own client runtime routes and client handlers outside core.

## Package entrypoint
- Entry point group: `xem.modules`
- Entry point name: `clients`
- Provider: `xem_module_clients.manifest:get_manifest`

## Local dev install
```bash
cd /Users/xtroverso/Documents/codex/xtroverso-analysis
.venv/bin/pip install -e module-repos/xem-module-clients
```
